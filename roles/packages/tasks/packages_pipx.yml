---
- name: "Install packages"
  ansible.builtin.command: |
    pipx install --global {{ item }}
  changed_when: true
  async: 120
  poll: 0
  loop: "{{ packages_pipx }}"
  loop_control:
    label: "{{ item }}"
  tags:
    - pipx
    - kali
    - packages
