---
- name: "Install packages"
  ansible.builtin.shell: |
    set -o pipefail
    pipx install --global {{ item }}
  changed_when: true
  async: 120
  poll: 0
  loop: "{{ packages_pipx }}"
  loop_control:
    label: "{{ item }}"
  tags:
    - pipx
    - kali
    - packages
